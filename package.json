{
  "name": "salesstock-pro",
  "version": "1.0.0",
  "description": "Sistema de Gestión de Ventas e Inventario con Firebase Auth y Firestore",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "verbose": true,
    "collectCoverageFrom": [
      "js/**/*.js",
      "app.js"
    ]
  }
}
